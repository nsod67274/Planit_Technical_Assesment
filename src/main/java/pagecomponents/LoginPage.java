package pagecomponents;

import annotations.Page;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

@Page
public class LoginPage {
    public LoginPage(WebDriver driverThreadLocal, By header) {
    }
}
